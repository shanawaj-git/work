import { defineMessages } from 'react-intl';

const scope = 'orders.payment.form';

export default defineMessages({
  continue: {
    id: `${scope}.continue`,
    defaultMessage: 'Continue',
  },
});
